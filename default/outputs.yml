outputs:
  telescope_otlp_demo:
    systemFields:
      - cribl_pipe
    streamtags: []
    signatureVersion: v4
    reuseConnections: true
    rejectUnauthorized: true
    enableAssumeRole: false
    durationSeconds: 3600
    stagePath: $CRIBL_HOME/state/outputs/staging
    addIdToStagePath: true
    objectACL: private
    removeEmptyDirs: true
    baseFileName: "`CriblOut`"
    fileNameSuffix: '`.${C.env["CRIBL_WORKER_ID"]}.${__format}${__compression ===
      "gzip" ? ".gz" : ""}`'
    maxFileSizeMB: 64
    maxOpenFiles: 100
    headerLine: ""
    writeHighWaterMark: 64
    onBackpressure: block
    deadletterEnabled: false
    onDiskFullBackpressure: block
    maxFileOpenTimeSec: 300
    maxFileIdleTimeSec: 300
    verifyPermissions: true
    maxClosingFilesToBackpressure: 100
    awsAuthenticationMethod: auto
    maxConcurrentFileParts: 1
    dynamicDataset: false
    emptyDirCleanupSec: 300
    type: cribl_lake
    description: storage destination for https://opentelemetry.io/docs/demo/
    destPath: local_otel_demo
    compress: gzip
